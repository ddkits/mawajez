{
  "name": "mawajez",
  "private": true,
  "version": "1.1.0",
  "scripts": {
    "dev": "vite",
    "build": "npm run update:sitemap && vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist",
    "generate:indexes": "node scripts/generate-indexes.js",
    "generate:sitemap": "node scripts/generate-sitemap.js",
    "update:sitemap": "npm run generate:indexes && npm run generate:sitemap"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "fast-xml-parser": "^5.3.1",
    "gh-pages": "^6.3.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-helmet-async": "^2.0.5",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^7.2.2"
  }
}
